# Custom Item module
# Master on/off switch. With this False the module does nothing, its item is not
# loaded, and the server is stock. Set it to True and restart to load the item
# and register the .token voiced command.
Enabled = False

# How many tokens .token grants per use.
GrantCount = 1
