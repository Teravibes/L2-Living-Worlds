# Brain service dependencies, pinned for reproducible installs (FPC-047).
#
# Unpinned deps meant a later reinstall of the same Living World release could pull different versions and behave
# differently. These are the versions the brain and its test suite are validated against. When preparing a release,
# re-validate against the deployment host and update these pins with `pip freeze` if the host must match exactly.
flask==3.1.3
openai==3.15.0
python-dotenv==1.2.3
